wg-private-key: UKizDngyf0LqmV+Z/wV4l/5+QChPz4uSaTCscij+E0U=
navidrome-env: |
    ND_LASTFM_ENABLED=true
    ND_LASTFM_APIKEY=a1f548cf129873dd7f56ccc583704e5e
    ND_LASTFM_SECRET=8ff5a6d614830d840b1d5c25bcde815e
xray-config: |
    {
      "log": {
        "loglevel": "warning" // Уровень детализации логов: "none", "error", "warning", "info", "debug" (от меньшего к большему)
      },

      "dns": {
        "servers": [
          "localhost"
        ]
      },

      "inbounds": [
        {
          "tag": "socks-in",
          "protocol": "socks",
          "listen": "127.0.0.1",
          "port": 10800,
          "settings": {
            "udp": true
          }
        },

        {
          "tag": "http-in",
          "protocol": "http",
          "listen": "127.0.0.1",
          "port": 10801
        }
      ],

      "outbounds": [
        {
          "tag": "proxy",
          "protocol": "vless",
          "settings": {
            "vnext": [
              {
                "address": "xray.kcu.su",
                "port": 57625,
                "users": [
                  {
                    "id": "2ec7d76c-f9f7-42ef-8746-afd886ebaa19",
                    "encryption": "none"
                  }
                ]
              }
            ]
          },
          "streamSettings": {
            "network": "tcp",
            "security": "reality",
            "realitySettings": {
              "serverName": "telegram.org",
              "fingerprint": "chrome",
              "publicKey": "UKzjpTzjK_uNQeM97IdsMCU2jRkmkgWKK6FORRDi7zg",
              "shortId": "a1",
              "spiderX": "/"
            },
            "sockopt": {
              "mark": 128
            }
          }
        },
        {
          "tag": "direct",
          "protocol": "freedom"
        },
        {
          "tag": "block",
          "protocol": "blackhole"
        }
      ]
    }
soularr-config: "[Lidarr]\n# Get from Lidarr: Settings > General > Security\napi_key = 2250aeee6ed34c41940b9627ee32ab02\n# URL Lidarr uses (e.g., what you use in your browser)\nhost_url = http://localhost:8686\n# Path to slskd downloads inside the Lidarr container\ndownload_dir = /srv/files/slskd\n# If true, Lidarr won't auto-import from Slskd\ndisable_sync = False\n\n[Slskd]\n# Create manually (see docs)\napi_key = mahChufaeweipemeeheexaoDahchohZi\n# URL Slskd uses\nhost_url = http://localhost:5030\nurl_base = /\n# Download path inside Slskd container\ndownload_dir = /downloads\n# Delete search after Soularr runs\ndelete_searches = True\n# Max seconds to wait for downloads (prevents infinite hangs)\nstalled_timeout = 3600\n\n[Release Settings]\n# Pick release with most common track count\nuse_most_common_tracknum = True\nallow_multi_disc = True\n# Accepted release countries\naccepted_countries = Europe,Japan,United Kingdom,United States,[Worldwide],Australia,Canada,Russia\n# Accepted formats\naccepted_formats = CD,Digital Media,Vinyl\n\n[Search Settings]\nsearch_timeout = 5000\nmaximum_peer_queue = 50\n# Minimum upload speed (bits/sec)\nminimum_peer_upload_speed = 0\n# Minimum match ratio between Lidarr track and Soulseek filename\nminimum_filename_match_ratio = 0.8\n# Preferred file types and qualities (most to least preferred)\n# Use \"flac\" or \"mp3\" to ignore quality details\nallowed_filetypes = flac 24/192,flac 16/44.1,flac\n# Set to False to only search for album titles (Note Soularr does not search for individual tracks, this setting searches for track titles but still tries to match to the full album). \nsearch_for_tracks = True\n# Prepend artist name when searching for albums\nalbum_prepend_artist = True\ntrack_prepend_artist = True\n# Search modes: all, incrementing_page, first_page\n# \"all\": search for every wanted record, \"first_page\": repeatedly searchs the first page, \"incrementing_page\": starts with the first page and increments on each run.\nsearch_type = incrementing_page\n# Albums to process per run\nnumber_of_albums_to_grab = 10\n# Unmonitor album on failure; logs to failure_list.txt\nremove_wanted_on_failure = False\n# Blacklist words in album or track titles (case-insensitive)\n# Lidarr search source: \"missing\" or \"cutoff_unmet\"\nsearch_source = missing\n# ignored_users = \n\n[Logging]\n# Passed to Python's logging.basicConfig()\n# See: https://docs.python.org/3/library/logging.html\nlevel = INFO\nformat = [%(levelname)s|%(module)s|L%(lineno)d] %(asctime)s: %(message)s\ndatefmt = %Y-%m-%dT%H:%M:%S%z\n"
slskd-env: |
    SLSKD_API_KEY=mahChufaeweipemeeheexaoDahchohZi
    SLSKD_SLSK_USERNAME=spl3g
    SLSKD_SLSK_PASSWORD=j9q5r@r7!dOES
    SLSKD_USERNAME=spl3g
    SLSKD_PASSWORD=j9q5r@r7!dOESo
explo-env: |
    # === Discovery Config ===

    # Service which recommends songs (only 'listenbrainz' is supported)
    # DISCOVERY_SERVICE=listenbrainz
    # Your ListenBrainz username
    LISTENBRAINZ_USER=spl3g
    # 'playlist' to fetch weekly playlist (50 songs), 'api' for fewer songs (good for testing) (default: playlist)
    # LISTENBRAINZ_DISCOVERY=api

    # === Music System Configuration ===

    # Music system you use: emby, jellyfin, mpd, plex or subsonic
    EXPLO_SYSTEM=subsonic
    # Address of your media system (e.g. http://127.0.0.1:4533)
    SYSTEM_URL=http://127.0.0.1:4533
    # Username with access to system (required for all except mpd)
    SYSTEM_USERNAME=spl3g
    # Password for the user (required for subsonic, recommended for plex)
    SYSTEM_PASSWORD=carlysimon
    # Mark playlist as public (subsonic)
    # PUBLIC_PLAYLIST=false

    # === Downloader Configuration ===

    # Directory to store downloaded tracks. It's recommended to make a separate directory (under the music library) for Explo
    # Download/move tracks to a subdirectory named after the playlist
    # USE_SUBDIRECTORY=true
    # Keep original file permissions when moving files (set to false on Synology devices)
    # KEEP_PERMISSIONS=true
    # Rename migrated track in {artist}-{title} format
    # RENAME_TRACK=true
    # Comma-separated list (no spaces) of download services, in priority order (default: youtube)
    DOWNLOAD_SERVICES=slskd,youtube

    # Directory for writing .m3u playlists (required only for MPD)
    # PLAYLIST_DIR=/path/to/playlist/folder/

    # === YouTube Configuration ===

    # YouTube Data API key (required if using youtube)
    # YOUTUBE_API_KEY=
    # Custom path to ffmpeg binary (default: defined in $PATH)
    # FFMPEG_PATH=
    # Custom path to yt-dlp binary (default: defined in $PATH)
    # YTDLP_PATH=
    # Path to (optional) cookies file (default ./cookies.txt) (in docker this is set through volume mapping)
    # COOKIES_PATH=./cookies.txt
    # Comma-separated (without spaces) keywords to exclude from YouTube results (default: live,remix,instrumental,extended,clean,acapella)
    # FILTER_LIST=live,remix,instrumental,extended

    # === Slskd Configuration ===

    # Slskd instance address (requires running instance)
    SLSKD_URL=http://localhost:5030
    # Slskd API key
    SLSKD_API_KEY=mahChufaeweipemeeheexaoDahchohZi
    # Whether to move downloads under the DOWNLOAD_DIR or not (default: false)
    MIGRATE_DOWNLOADS=true
    # Directory where slskd downloads tracks (default: /slskd/)
    # PS! This is only needed on the binary version, in docker it's set through volume mapping
    # SLSKD_DIR=/slskd/
    # Number of times to check search status before skipping the track (default: 5)
    # SLSKD_RETRY=5
    # Number of download attempts for a track (default: 3)
    # SLSKD_DL_ATTEMPTS=3

    ## Slskd Filtering

    # Comma-separated (without spaces) file extensions to download from (default: flac,mp3)
    # EXTENSIONS=flac,mp3
    # Minimal Bit Depth (default: 8)
    # MIN_BIT_DEPTH=8
    # Minimal Bitrate (default: 256)
    # MIN_BITRATE=256
    # Comma-separated (without spaces) keywords to avoid, when filtering slskd results (default: live,remix,instrumental,extended,clean,acapella)
    # FILTER_LIST=live,remix,instrumental,extended,clean,acapella

    # === Metadata / Formatting ===

    # Set to true to merge featured artists into title (recommended), false appends them to artist field (default: true)
    # SINGLE_ARTIST=true

    # === Misc ===

    # Minutes to sleep between library scans (default: 2)
    # SLEEP=2
    # Set the log level (DEBUG, INFO, WARN, ERROR) (default: INFO)
    # LOG_LEVEL=INFO
    # Set a custom HTTP timeout for music servers (in seconds) (default: 10)
    # CLIENT_HTTP_TIMEOUT=10
